# Evening Anchor 代码审查 & 冗余清理

**时间**：2026-05-03 18:08 CST
**文件**：E:\projects\evening-anchor\index.html

## 清理结果

**修改前**：2591 行 / 112,727 字节
**修改后**：2541 行 / 110,809 字节
**减少**：50 行 / 1,918 字节

## 已删除的冗余

| # | 类型 | 内容 | 原因 |
|---|------|------|------|
| 1 | 死函数 | `addMinutesWithFlag()` | 返回对象但无调用者 |
| 2 | 死函数 | `addMinutes()` | 仅被 `addMinutesWithFlag` 调用（已删） |
| 3 | 死函数 | `timeRange()` | 0处调用，返回 start/end 对象 |
| 4 | 死函数 | `parseTime()` | 与 `parseTimeHM()` 完全重复，0处调用 |
| 5 | 重复函数 | `_highlightActive()` | 与 `_updateActive()` 完全相同，合并为一个 |
| 6 | 死代码 | `goalConfig.*.blocks` 回调 | 4个goal的blocks都返回空数组，从未被调用 |
| 7 | 重复数据 | TASK_LIBRARY "洗个热水澡" | 保留更详细的版本 |
| 8 | 重复数据 | TASK_LIBRARY "泡脚 + 拉伸" | 保留更详细的版本 |
| 9 | 重叠数据 | TASK_LIBRARY "🧘冥想"×2 | 保留更详细的版本 |
| 10 | 重叠数据 | TASK_LIBRARY "😴小睡"×2 | 保留更详细的版本 |
| 11 | 重叠数据 | TASK_LIBRARY "☀️散步"×2 | 保留更详细的版本 |
| 12 | 重叠数据 | TASK_LIBRARY "👂播客"×2 | 保留更详细的版本 |

## 保留的函数（经确认仍有调用）

- `clockMins()` → 被 `minutesBetween()` 内部调用
- `minutesBetween()` → 被 `renderPlan()` 和 `copyPlan()` 调用
- `tagClass()` → 被 `renderPlan()` 调用
- `estimateDuration()` → 被 `buildPlan()` 内部调用
- `parseTimeHM()` → 被 `buildPlan()`、`buildLowPowerPlan()`、`startLiveTimer()` 调用
- `escapeHtml()` → 被 `renderPlan()` 调用
